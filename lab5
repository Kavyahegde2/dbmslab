
















-----------5---------------
SELECT DNO, COUNT(*)
FROM DEPARTMENT NATURAL JOIN EMPLOYEE 
SALARY > 600000
AND DNO IN (SELECT DNO
FROM EMPLOYEE E1
GROUP BY DNO
HAVING COUNT(*)>5)
GROUP BY DNO;
